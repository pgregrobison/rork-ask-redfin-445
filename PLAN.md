# Pan without zooming when tapping a map pin

**What changes:**
- When you tap a pin on the map, the map will smoothly pan to center on that pin **without changing the zoom level**
- The current zoom level is preserved exactly as-is
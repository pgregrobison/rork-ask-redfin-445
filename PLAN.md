# Position the tour day notification just below the Dynamic Island

**Change**

- Move the fake tour day notification down so it sits just below the Dynamic Island / status area, matching where real iOS notifications appear, instead of overlapping the very top of the screen.
- Keep all existing behavior: tap to open the tour day thread, swipe up to dismiss, auto-dismiss timer, and the spring-in animation.
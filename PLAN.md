# Red favorite heart & adjust tag padding on map home card

**Changes**

1. **Red favorite heart** — When the heart icon is tapped (saved), it will turn red instead of the current default fill color.

2. **Tag padding on map home card** — Add 8pt of left and top padding to the badge/tags in the upper-left corner of the map home card (the `ListingCardOverlay`) so they sit comfortably inside the larger corner radius.
# Top-align map controls with the list/map toggle in accessory mode

**What changes**

- In the accessory variant of Find, the floating map controls (layers, draw, locate) currently sit just below the top safe area, which used to line up with the profile button on the right.
- Since the profile button no longer exists in accessory mode, the controls now sit lower than the list/map toggle on the left.
- I'll shift the map controls up so their top edge aligns with the list/map toggle in the navigation bar, giving a clean, balanced top row.
- The non-accessory variant (where the profile button still exists) will be left unchanged.
# Remove the active filter chips row

**Change**
- Remove the row of active filter chips that appears below the location bar on the Find screen, since it's interfering with the layout.

Nothing else changes — filters still work the same way, they just won't show as pills on top of the map anymore.
# Fix Ask Redfin header: remove background and fix dropdown color

**Changes:**

1. **Remove solid background from the header area** — Make the header bar transparent so it doesn't have a visible solid background color behind it
2. **Fix dropdown accent color** — The thread switcher dropdown menu is currently using the system accent blue for its text and icon. This will be updated to use system color (black in light mode, white in dark mode) to match the close button
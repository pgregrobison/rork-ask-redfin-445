# Detail page styling tweaks — links, bars, spacing, tabs


**Changes**

- **Link text color**: Change the text-link button style from brand red to primary (system black/white) so it blends more naturally
- **Payment bar gap**: Increase the gap between colored bars in the payment calculator from 2pt to 4pt
- **Payment header spacing**: Increase the space between the "$X,XXX /mo" header and the breakdown table to the next spacing token up (32pt instead of 24pt)
- **Ask Redfin description**: Remove "Let's dive in!" from the end of the description text
- **Media tab labels**: Restore text labels below each icon in the segmented tab bar — "Photos", "Map", "3D", "Street View". Also add a map pin icon for the second tab (it was missing its icon)
